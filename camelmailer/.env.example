# Copy to .env and adjust. Only POSTGRES_PASSWORD is required.

# PostgreSQL password for the bundled database container (required).
POSTGRES_PASSWORD=

# Host ports (defaults shown).
#WEB_PORT=5000
#SMTP_PORT=25

# Log level for all CamelMailer processes.
#RUST_LOG=info
